<%= simple_form_for([@list], local: true) do |form| %>
  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name, id: :list_name %>
    <%= f.input :photo, as: :file %>

  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
